package org.burgers.spring.cache

enum TimeUnit {
    SECONDS(Calendar.SECOND), MINUTES(Calendar.MINUTE), HOURS(Calendar.HOUR)

    private final value

    TimeUnit(int value) {
        this.value = value
    }

    public value() {
        return value
    }
}
